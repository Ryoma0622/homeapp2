<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.home.expenses.repository.UserRoleRepository">
  <select id="findById" resultType="com.home.expenses.model.UserRole">
    SELECT * FROM user_role WHERE id = #{id}
  </select>
</mapper>